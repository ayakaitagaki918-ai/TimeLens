# プロジェクト概要
TimeLens は時間管理・可視化を目的としたWebアプリです。

## 技術スタック
- HTML / CSS / JavaScript

## コーディング規約
- コメントは日本語で書く
- 変数名は英語のキャメルケース（例：taskName）を使う
- インデントはスペース2つ
- ファイル名はケバブケース（例：time-tracker.js）を使う

## 返答ルール
- 返答は必ず日本語で行う
- コードはコードブロックで出力する

## Git 運用ルール
- コードを変更するたびに、必ずGitHubにプッシュする
- コミットメッセージは日本語で、変更内容がわかるように書く（例：「タイマー機能を追加」）
- プッシュ前に `git status` で変更内容を確認する
- 手順：`git add .` → `git commit -m "メッセージ"` → `git push`

## 禁止事項
- `rm -rf` コマンドは絶対に実行しない
- `.env` ファイルを読んだり変更したりしない
- データベースへの削除操作（DELETE文）を実行しない
- `package.json` の依存パッケージを無断で変更しない
